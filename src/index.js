import React from 'react';
import ReactDOM from 'react-dom/client';
import CatOS from './CatOS';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<React.StrictMode><CatOS /></React.StrictMode>);
